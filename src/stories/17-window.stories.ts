import { WindowComponent } from '../app/window/window.component';

export default {
    title: 'Window',
    component: WindowComponent,
};

export const Base = () => ({
    component: WindowComponent,
});
