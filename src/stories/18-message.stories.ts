import { MessageComponent } from '../app/message/message.component';

export default {
    title: 'Message',
    component: MessageComponent,
};

export const Base = () => ({
    component: MessageComponent,
});
