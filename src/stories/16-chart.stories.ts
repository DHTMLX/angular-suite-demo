import { ChartComponent } from '../app/chart/chart.component';

export default {
    title: 'Chart',
    component: ChartComponent,
};

export const Base = () => ({
    component: ChartComponent,
});
